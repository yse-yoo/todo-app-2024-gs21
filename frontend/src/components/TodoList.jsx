import React from 'react'

function TodoList() {
  return (
    <div>
        <h2>Todo List</h2>
        <input type="text" placeholder="Add a new todo." />
        <button>Add</button>
    </div>
  )
}

export default TodoList